name: 01-env-variables
on:
  workflow_dispatch
env:
    ENVIRONMENT: "dev"
    APP_NAME: Academy
jobs:
    env-demo:
       runs-on: ubuntu-latest
       steps:
       - name: Print Variables
         run: |
           echo "printing environmnental variables"
           echo "Application Name is : $APP_NAME"
           echo "Environment Name is : $ENVIRONMENT"

name: First Workflow
on: 
   Workflow_dispatch:
jobs:
   first-job:
    runs-on: ubuntu-latest
    steps:
      -name: Print Welcome message
      run: echo "Welcome everyone to github actions course"